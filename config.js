import 'dotenv/config';

export default {
    client_id: '' || process.env.CLIENT_ID,
    client_secret: '' || process.env.CLIENT_SECRET,
    redirect_uri: '' || process.env.REDIRECT_URI,
    passwrod_session: '' || process.env.PASSWORD_IRON_SESSION,
};
